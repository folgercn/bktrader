# 2026-04-07 Testnet 最小闭环进度

## 目标
先完成最小实现闭环：
- 在开发目录直接启动后端
- 使用 `.env` 提供配置
- 连通 Binance Futures testnet
- 完成最小真实下单、同步、持仓更新
- 后续再逐步接 runtime / 策略自动化 / 风控

## 本次结论
截至 2026-04-07，**最小闭环已打通**，并且不是 mock，而是 **真实 Binance Futures testnet REST**。

已验证成功的链路：
1. API 启动成功
2. 登录鉴权成功
3. 创建 LIVE 账户成功
4. 绑定 `binance-futures` 成功
5. 使用 `sandbox=true + executionMode=rest` 成功连接 Binance Futures testnet
6. 账户同步成功（可读到真实 testnet 余额）
7. 最小真实订单提交成功
8. 订单 `sync` 成功
9. `fills` / `positions` 已更新

---

## 调研与验证过程摘要

### 1. 项目结构确认
后端主入口：
- `cmd/platform-api`

当前配置样例：
- `configs/app.example.env`

当前生产部署相关：
- `deployments/docker-compose.prod.yml`
- `scripts/deploy.sh`
- `.github/workflows/ci.yml`
- `.github/workflows/cd.yml`

### 2. 运行方式判断
结论：
- **开发目录直接运行**：可通过 `source .env && go run ./cmd/platform-api` 启动
- **正式部署**：当前 CI/CD 走 docker compose + 外部 env 文件

补充结论：
- `/Users/fujun/Downloads/actions-runner-bktrader` 是 GitHub Actions runner 目录
- `/Users/fujun/services/bktrader` 是当前 workflow 配置中的部署目标目录
- 开发阶段本次工作选择 **直接在仓库目录 `/Users/fujun/node/bktrader` 内验证**

### 3. `.env` 状态检查
已检查开发目录 `.env`，关键项齐全且非空，包括：
- `HTTP_ADDR`
- `STORE_BACKEND`
- `AUTH_ENABLED`
- `AUTH_USERNAME`
- `AUTH_PASSWORD`
- `AUTH_SECRET`
- `BINANCE_TESTNET_API_KEY`
- `BINANCE_TESTNET_API_SECRET`

### 4. mock 闭环验证
先完成了平台内部 mock 闭环验证：
- 创建 LIVE 账户
- 绑定 `sandbox=true + executionMode=mock`
- 通过 `strategyVersionId + metadata.manualTest=true` 提交订单
- 调用 `sync`
- 成功得到 fill 和 position 更新

这一步证明：
- 平台内部订单链路已通
- 订单/成交/持仓聚合逻辑工作正常

### 5. 为什么需要 `strategyVersionId`
对 `LIVE` 账户执行 `POST /api/v1/orders` 时，系统要求：
- 要么提供 `strategyVersionId`
- 要么账户已经挂了 linked runtime session

否则会报错：
- `live order requires strategyVersionId or a linked runtime session`

为了做最小烟雾测试，本次采用：
- 提供现有策略版本 ID
- 同时加 `metadata.manualTest=true`

这样可以跳过 runtime readiness 检查，先验证真实交易链路。

### 6. 使用的策略版本
当前使用的策略版本：
- `strategy-version-bk-1d-v010`

对应策略：
- `strategy-bk-1d`
- `BK 1D Zero Initial`

### 7. Binance Futures testnet REST 验证
在开发目录使用 `.env` 启动服务后，完成了真实 testnet 账户同步：
- 可成功读取 testnet 账户资产
- 返回的 REST 基地址为：
  - `https://testnet.binancefuture.com`

说明以下几项已成立：
- API Key / Secret 可用
- Binance 签名逻辑可用
- 网络连通正常
- testnet REST 下单通道可用

### 8. 首次真实下单失败原因
首次尝试下单：
- `BTCUSDT`
- `MARKET`
- `quantity=0.001`

Binance 返回：
- `code=-4164`
- `Order's notional must be no smaller than 100`

这说明请求已经真正打到 Binance testnet 下单接口，失败原因不是程序错误，而是交易所最小名义价值限制。

### 9. 成功打通的真实 testnet 实单
后续改为：
- `symbol=BTCUSDT`
- `side=BUY`
- `type=MARKET`
- `quantity=0.002`

成功结果：
- 订单状态：`FILLED`
- 交易所订单号：`13023423919`
- 成交均价：`68701.4`
- 手续费：`0.05496112`

同步后平台内状态：
- 生成 fill 记录
- 生成 LONG 持仓
- 持仓数量：`0.002`
- 持仓开仓价：`68701.4`

---

## 当前已确认能力

### 平台内部
- 账户创建
- live adapter 绑定
- 订单创建
- live submit
- live sync
- fill 落库
- position 更新

### 外部链路
- Binance Futures testnet 账户查询
- Binance Futures testnet 真实下单
- Binance Futures testnet 订单查询
- Binance trade report 回填

---

## 当前限制与注意事项

### 1. LIVE 下单前置约束
当前 LIVE 下单不能裸下，需满足其一：
- 传 `strategyVersionId`
- 或有 linked runtime session

### 2. 最小烟雾测试依赖 `manualTest=true`
当前为了跳过 runtime readiness 检查，使用：
- `metadata.manualTest=true`

这适合做开发阶段最小连通性测试，但不是最终自动运行方案。

### 3. Binance testnet 最小名义价值限制
对 BTCUSDT 之类合约，订单名义价值需满足交易所规则。
当前 `0.001 BTC` 会卡在边界或不足，`0.002 BTC` 已验证可成交。

### 4. 当前阶段仍未接通的部分
以下内容还未纳入“已完成”范围：
- runtime 自动拉起
- 实时行情驱动策略评估
- 自动 dispatch
- 完整风控拦截
- 持久化部署验证（Postgres/Compose 正式路径）
- 前端控制台联调

---

## 建议的下一阶段任务

### 阶段 1：把最小闭环固化
建议优先做：
1. 整理标准启动步骤
2. 固化 testnet smoke test 流程
3. 输出可复用脚本（如 `scripts/testnet_smoke.sh`）
4. 约定统一的 account / binding / symbol / quantity 参数

### 阶段 2：接回策略运行链路
在最小闭环已确认后，再推进：
1. 创建/拉起 runtime session
2. 接回 signal source
3. 校验 runtime readiness
4. 从 `manualTest` 过渡到真实 runtime 驱动下单

### 阶段 3：补风控与运维能力
后续再做：
1. 最小下单量、最小名义价值预检查
2. 错误分类与日志结构化
3. 账户状态同步与异常告警
4. Docker / CI-CD 正式路径收口

---

## 当前建议结论
当前最重要的结论只有一句：

> **开发目录 + `.env` + Binance Futures testnet REST + 最小真实订单** 已经完整跑通。

后续工作不再是“能不能跑起来”，而是：
- 固化流程
- 接回 runtime
- 再做策略和风控优化
