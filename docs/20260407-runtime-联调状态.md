# 2026-04-07 runtime 联调状态

## 本轮目标
在已完成 Binance Futures testnet 最小真实下单闭环的基础上，继续联调：
- 行情信号接入
- signal runtime 启动
- live session 启动
- 触发策略评估
- 生成 strategy decision / intent
- 为后续自动派单做准备

---

## 当前结论
截至当前时间，本轮联调已经推进到：

### 已通过
1. **开发联调环境已切到 Postgres 持久化**
   - 不再使用 `STORE_BACKEND=memory`
   - 避免服务中断后联调现场丢失
2. **本地 dev 依赖已启动**
   - Postgres
   - Redis
   - NATS
3. **数据库迁移已执行成功**
4. **开发服务已恢复并健康检查正常**
5. **testnet LIVE 账户已重建并绑定 REST testnet**
6. **账户同步成功**
   - 可读取真实 Binance Futures testnet 余额
7. **strategy / account signal bindings 已补齐**
   - `binance-trade-tick` 作为 `trigger`
   - `binance-order-book` 作为 `feature`
8. **runtime plan 已 READY**
9. **signal runtime session 已创建并启动成功**
10. **runtime 当前健康状态正常**
    - `status=RUNNING`
    - `health=healthy`
    - `connectedAt` 有值
    - `lastHeartbeatAt` 有值
11. **真实 websocket 行情已接入**
    - `btcusdt@trade`
    - `btcusdt@depth20@100ms`
12. **sourceStates 已更新**
    - `binance-trade-tick|trigger|BTCUSDT`
    - `binance-order-book|feature|BTCUSDT`
13. **live session 已创建并启动成功**
    - `status=RUNNING`
    - 已正确 linked 到 signal runtime
14. **live session 已收到 runtime 事件**
    - `lastSignalRuntimeEvent`
    - `lastSignalRuntimeEventAt`
    均有值

---

## 当前核心阻塞
虽然：
- runtime 已经健康运行
- 行情已真实接入
- live session 已收到 runtime 事件

但是：

### 仍未出现的结果
- `lastStrategySignal`
- `lastStrategyDecision`
- `lastStrategyIntent`

### 当前观测到的关键状态
runtime 侧：
- `strategySignalCount = 0`
- `lastStrategySignal = null`

live session 侧：
- 已 `RUNNING`
- 已 linked runtime
- 已看到 `lastSignalRuntimeEvent`
- 但仍没有策略评估结果字段落下来

---

## 结论判断
当前问题已经不再是：
- 环境问题
- 代理问题
- Binance testnet 问题
- websocket 连接问题
- sourceStates 缺失问题
- runtime readiness 问题

当前问题已经收敛为：

> **市场事件已经进入 runtime / live session，但没有真正触发策略评估产出。**

换句话说：

> **“行情 -> runtime -> live session” 已通，但 “事件 -> 策略评估 -> decision / intent” 这一层还没通。**

---

## 已完成的关键环境修正
本轮联调中，已完成一个重要修正：

### 从 memory 切到 postgres
原先开发目录使用：
- `STORE_BACKEND=memory`

问题：
- 服务一中断，账户、bindings、runtime session、live session 等联调现场全部丢失

现已改为：
- `STORE_BACKEND=postgres`

影响：
- 后续联调现场可保留
- 不再因为服务短暂中断而整轮重建

---

## 当前分支
本轮联调工作统一放在分支：

```bash
feature/runtime-integration-testnet
```

后续待业务逻辑打通后，再考虑合并。

---

## 下一步建议
后续恢复联调时，应只盯一个单点继续查：

### 单点目标
查清楚为什么：
- runtime 已收到 trigger / feature 事件
- 但没有产出 strategy signal
- 进而没有 `lastStrategyDecision` / `lastStrategyIntent`

### 建议排查方向
1. `live session` / `runtime session` 是在哪个代码路径触发策略评估
2. `trade_tick + order_book` 是否只用于更新状态，而不会直接触发策略评估
3. 当前策略是否还依赖 `signal_bar` / 时间计划等额外触发条件
4. 当前是否被以下 gate 拦住：
   - time gate
   - source gate
   - price gate
   - position gate
5. 是否需要额外创建/接入 `signal_bar` 才能真正推进策略状态机

---

## 当前一句话总结
当前联调状态可以概括为：

> **runtime 层已经打通，业务逻辑层还未打通。**

更具体地说：

> **行情已经进来了，但还没有变成策略 decision / intent。**
