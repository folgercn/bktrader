# main.tsx 拆解执行计划 — 超细粒度版

> **原则**: 每个步骤是一次最小化的 **剪切 → 粘贴 → 更新 import → 编译验证** 循环。
> 每完成一个 **区块 (Block)**，执行 `git add -A && git commit`，然后通知人工验证。

---

## 📋 当前文件结构扫描结果

| 行范围 | 内容 | 约行数 |
|---|---|---|
| 1-57 | import 语句 + 类型导入 | 57 |
| 58 | `API_BASE` 常量 | 1 |
| 61-85 | `readStoredAuthSession()` / `writeStoredAuthSession()` （**重复定义**，行 6 已 import） | 25 |
| 87-265 | `App()` 开头 — 180 行 store 解构 | 179 |
| 266-605 | derived state 计算 | 340 |
| 607-831 | `loadDashboard()` 异步函数 | 225 |
| 833-998 | `useEffect` / `useMemo` 钩子 | 165 |
| 1001-1821 | 24 个 action handler 函数 | 820 |
| 1837-3941 | JSX return — WorkbenchLayout 容器 | 2104 |
| 3946-4016 | Modal: Live Account | 70 |
| 4018-4061 | Modal: Login | 43 |
| 4063-4152 | Modal: Live Binding | 89 |
| 4154-4391 | Modal: Live Session | 237 |
| 4393-4464 | Modal: Telegram | 71 |
| 4466-4469 | `App()` 函数结尾 `</>` 和 `}` | 4 |
| 4472-4607 | `TradingChart` 组件 | 136 |
| 4609-4661 | `SignalBarChart` 组件 | 53 |
| 4663-4738 | `SignalMonitorChart` 组件 | 76 |
| 4740-4756 | `ActionButton` 组件 | 17 |
| 4758-4781 | `FilterGroup` 组件 | 24 |
| 4783-4790 | `MetricCard` 组件 | 8 |
| 4792-4794 | `StatusPill` 组件 | 3 |
| 4796-4823 | `SimpleTable` 组件 | 28 |
| 4825-4853 | `SampleCard` 组件 | 29 |
| 4856-4880 | `fetchJSON` 工具函数 | 25 |
| 4951-4994 | `ErrorBoundary` class 组件 | 44 |
| 4996-5003 | `ReactDOM.createRoot` 挂载 | 8 |

### 已有抽取 (不需要动)

| 文件 | 内容 |
|---|---|
| `src/components/SlideOver.tsx` | SlideOver 组件 |
| `src/layouts/WorkbenchLayout.tsx` | 布局壳 |
| `src/store/useUIStore.ts` | UI 状态 store |
| `src/store/useTradingStore.ts` | 交易数据 store |
| `src/utils/derivation.ts` | 派生计算函数 |
| `src/utils/format.ts` | 格式化工具 |
| `src/types/domain.ts` | 所有 TS 类型 |

### 目标新目录

```
src/
 ├── utils/api.ts                  # fetchJSON + API_BASE
 ├── components/ui/                # 6 个基础 UI 组件
 ├── components/charts/            # 3 个图表组件
 ├── components/ErrorBoundary.tsx   # ErrorBoundary
 ├── modals/                       # 5 个弹窗
 ├── panels/                       # 4 个底部面板
 └── pages/                        # 3 个页面 + 2 个主舞台
```

---

## Block A — 工具函数与基础原子组件 (最安全的底层代码)

> 这些代码不依赖 App 内部状态，是纯组件或纯函数，剪出去零风险。

### 步骤 A1: 删除重复的 auth 函数定义

- **操作**: 删除 `main.tsx` 第 61-85 行的 `readStoredAuthSession()` 和 `writeStoredAuthSession()` 函数定义（它们已经在 `src/utils/auth.ts` 中存在，且第 6 行已 import）。
- **注意**: 原文件第 6 行写的是 `import { readStoredAuthSession, writeStoredAuthSession } from './utils/auth'`，但第 61 行又用 `export function readStoredAuthSession()` 重新定义了——这会导致 shadow 覆盖 import。删除本地定义后，import 生效即可。
- **验证**: `npx tsc --noEmit` 编译通过

### 步骤 A2: 抽取 `fetchJSON` → `src/utils/api.ts`

- **操作**:
  1. 创建 `src/utils/api.ts`
  2. 把 `main.tsx` 第 58 行的 `API_BASE` 常量和第 4856-4880 行的 `fetchJSON` 函数剪切过去
  3. 在 `api.ts` 中 import `readStoredAuthSession` from `'./auth'`
  4. 导出 `API_BASE` 和 `fetchJSON`
  5. 在 `main.tsx` 中添加 `import { API_BASE, fetchJSON } from './utils/api'`，删除原有代码
- **验证**: `npx tsc --noEmit`

### 步骤 A3: 抽取 `ErrorBoundary` → `src/components/ErrorBoundary.tsx`

- **操作**:
  1. 创建 `src/components/ErrorBoundary.tsx`
  2. 把第 4951-4994 行的 `ErrorBoundary` class 组件剪切过去
  3. 在新文件中 `import React from 'react'`，`export class ErrorBoundary`
  4. 在 `main.tsx` 尾部挂载处（第 4996-5003 行）改为 `import { ErrorBoundary } from './components/ErrorBoundary'`
  5. 删除 main.tsx 中原有代码
- **验证**: `npx tsc --noEmit`

### 步骤 A4: 抽取 `ActionButton` → `src/components/ui/ActionButton.tsx`

- **操作**:
  1. 创建 `src/components/ui/ActionButton.tsx`
  2. 把第 4740-4756 行的 `ActionButton` 函数剪切过去, `export function ActionButton`
  3. 在 `main.tsx` 中添加 `import { ActionButton } from './components/ui/ActionButton'`
- **验证**: `npx tsc --noEmit`

### 步骤 A5: 抽取 `StatusPill` → `src/components/ui/StatusPill.tsx`

- **操作**: 同上模式。源码在第 4792-4794 行（3 行）
- **依赖**: 仅 React
- **验证**: `npx tsc --noEmit`

### 步骤 A6: 抽取 `MetricCard` → `src/components/ui/MetricCard.tsx`

- **操作**: 同上模式。源码在第 4783-4790 行（8 行）
- **验证**: `npx tsc --noEmit`

### 步骤 A7: 抽取 `FilterGroup` → `src/components/ui/FilterGroup.tsx`

- **操作**: 同上模式。源码在第 4758-4781 行（24 行）
- **验证**: `npx tsc --noEmit`

### 步骤 A8: 抽取 `SimpleTable` → `src/components/ui/SimpleTable.tsx`

- **操作**: 同上模式。源码在第 4796-4823 行（28 行）
- **验证**: `npx tsc --noEmit`

### 步骤 A9: 抽取 `SampleCard` → `src/components/ui/SampleCard.tsx`

- **操作**: 同上模式。源码在第 4825-4853 行（29 行）
- **依赖**: import `sampleStatus` from `../utils/derivation`，import `formatMaybeNumber` from `../utils/format`，import `ReplaySample` from `../types/domain`
- **验证**: `npx tsc --noEmit`

> [!TIP]
> 步骤 A4-A9 可以一次性全部做完再编译一次——因为它们都是文件末尾的独立函数，彼此无依赖。但为了更安全，建议做一个编译一个。

---

### 🔖 Block A 完成

```bash
git add -A && git commit -m "refactor: extract utility functions and 6 UI primitives from main.tsx"
```

**⏸️ 暂停，通知人工验证：打开页面确认所有 UI 正常渲染。**

---

## Block B — 图表组件抽取

### 步骤 B1: 抽取 `TradingChart` → `src/components/charts/TradingChart.tsx`

- **操作**:
  1. 创建 `src/components/charts/TradingChart.tsx`
  2. 把第 4472-4607 行剪切过去
  3. 新文件需要的 imports:
     ```ts
     import { useEffect, useRef } from 'react'
     import { Time, CandlestickSeries, ColorType, CrosshairMode, LineStyle, createChart, createSeriesMarkers } from 'lightweight-charts'
     import { ChartCandle, ChartAnnotation, MarkerDetail } from '../../types/domain'
     import { markerPosition, markerColor, markerShape, markerText, findNearestAnnotation, toMarkerDetail } from '../../utils/derivation'
     ```
  4. 导出 `export function TradingChart`
  5. 在 `main.tsx` 中 `import { TradingChart } from './components/charts/TradingChart'`
- **验证**: `npx tsc --noEmit`

### 步骤 B2: 抽取 `SignalBarChart` → `src/components/charts/SignalBarChart.tsx`

- **操作**: 同上模式。源码在第 4609-4661 行
- **依赖**: `lightweight-charts` 的多个导出 + `SignalBarCandle` type + `applyDefaultChartWindow` from derivation
- **验证**: `npx tsc --noEmit`

### 步骤 B3: 抽取 `SignalMonitorChart` → `src/components/charts/SignalMonitorChart.tsx`

- **操作**: 同上模式。源码在第 4663-4738 行
- **依赖**: 同 B2 + `SessionMarker` type
- **验证**: `npx tsc --noEmit`

---

### 🔖 Block B 完成

```bash
git add -A && git commit -m "refactor: extract 3 chart components from main.tsx"
```

**⏸️ 暂停，通知人工验证：确认图表正常渲染、K线加载、标注点击等交互正常。**

---

## Block C — 弹窗 / Modal 抽取

> 这些弹窗是 App return 中最外层的条件渲染块，它们使用 App 内部大量的 state 和 action handler。
> **策略**: 每个 Modal 组件通过 props 接收所需的 state 和回调函数，不在 Modal 内部直接读取 store。

> [!IMPORTANT]
> 每个弹窗组件需要定义清晰的 Props 接口。操作者需要：
> 1. 阅读弹窗的 JSX，找出它引用了哪些变量和函数
> 2. 把这些变量全部定义为 props
> 3. 在 App 中将弹窗替换为 `<XxxModal prop1={...} prop2={...} />`

### 步骤 C1: 抽取 Login Modal → `src/modals/LoginModal.tsx`

- **源码范围**: 第 4018-4061 行 (`{!authSession ? (...) : null}`)
- **读取的外部变量**: `authSession`, `error`, `loginForm`, `loginAction`
- **调用的外部函数**: `setLoginForm`, `login`
- **引用的子组件**: `ActionButton`
- **操作**:
  1. 创建 `src/modals/LoginModal.tsx`
  2. 定义 `LoginModalProps` 接口包含上述所有变量和函数
  3. 在 App 中替换为 `<LoginModal ... />`
- **验证**: `npx tsc --noEmit`

### 步骤 C2: 抽取 Live Account Modal → `src/modals/LiveAccountModal.tsx`

- **源码范围**: 第 3946-4016 行 (`{activeSettingsModal === "live-account" ? (...) : null}`)
- **读取的外部变量**: `quickLiveAccount`, `quickLiveAccountId`, `liveAccounts`, `liveAccountForm`, `liveCreateAction`, `liveAccountError`, `liveAccountNotice`
- **调用的外部函数**: `setActiveSettingsModal`, `selectQuickLiveAccount`, `setLiveAccountForm`, `createLiveAccount`, `openLiveBindingModal`
- **引用的子组件**: `ActionButton`
- **验证**: `npx tsc --noEmit`

### 步骤 C3: 抽取 Live Binding Modal → `src/modals/LiveBindingModal.tsx`

- **源码范围**: 第 4063-4152 行
- **读取的外部变量**: `liveAccounts`, `liveAdapters`, `liveBindingForm`, `liveBindAction`, `liveBindingError`, `liveBindingNotice`, `quickLiveAccount`
- **调用的外部函数**: `setActiveSettingsModal`, `setLiveBindingForm`, `bindLiveAccount`
- **引用的子组件**: `ActionButton`
- **验证**: `npx tsc --noEmit`

### 步骤 C4: 抽取 Live Session Modal → `src/modals/LiveSessionModal.tsx`

- **源码范围**: 第 4154-4391 行（最大的弹窗，237 行）
- **读取的外部变量**: `liveAccounts`, `liveSessionForm`, `liveSessionCreateAction`, `liveSessionLaunchAction`, `liveSessionAction`, `liveSessionError`, `liveSessionNotice`, `editingLiveSessionId`, `strategyOptions`, `validLiveSessions`
- **调用的外部函数**: `setActiveSettingsModal`, `setLiveSessionForm`, `saveLiveSession`, `createAndStartLiveSession`, `setLiveSessionLaunchAction`, `setLiveSessionAction`, `setLiveSessionError`, `loadDashboard`, `setError`, `fetchJSON`（Save & Start 逻辑中直接调用了 fetchJSON）
- **引用的子组件**: `ActionButton`
- **注意**：Save & Start 按钮的 `onClick` 内联了一段 async 逻辑（第 4364-4385 行），建议保持为内联或提取为 props 回调 `onSaveAndStart`
- **验证**: `npx tsc --noEmit`

### 步骤 C5: 抽取 Telegram Modal → `src/modals/TelegramModal.tsx`

- **源码范围**: 第 4393-4464 行
- **读取的外部变量**: `telegramConfig`, `telegramForm`, `telegramAction`
- **调用的外部函数**: `setActiveSettingsModal`, `setTelegramForm`, `saveTelegramConfig`, `sendTelegramTest`
- **引用的子组件**: `ActionButton`
- **验证**: `npx tsc --noEmit`

---

### 🔖 Block C 完成

```bash
git add -A && git commit -m "refactor: extract 5 modal dialogs from main.tsx into src/modals/"
```

**⏸️ 暂停，通知人工验证：点击"设置 → 新建账户"、"绑定账户"、"创建会话"、"Telegram 通知"等弹窗，确认正常弹出、表单交互和提交。退出登录确认登录弹窗正常。**

---

## Block D — 底部面板 (Dock Panels) 抽取

> 这 4 个 panel 被包在 `dockContent={}` 这个 prop 中，用 `display: dockTab === 'xxx' ? 'block' : 'none'` 控制显隐。

### 步骤 D1: 抽取 Orders Panel → `src/panels/OrdersPanel.tsx`

- **源码范围**: 第 2731-2915 行（整个 `dockTab === 'orders'` 块）
- **包含内容**:
  - 创建 Live Order 表单
  - Live Execution Context 信息展示
  - 订单列表 SimpleTable
- **读取的外部变量**: `liveAccounts`, `strategies`, `liveOrderForm`, `liveOrderAction`, `orders`, `liveSyncAction`, 以及大量 `selectedLiveOrder*` 系列的派生状态
- **调用的外部函数**: `setLiveOrderForm`, `createLiveOrder`, `syncLiveOrder`
- **引用的子组件**: `ActionButton`, `StatusPill`, `MetricCard`, `SimpleTable`
- **操作**:
  1. 创建 `src/panels/OrdersPanel.tsx`
  2. 将该区间 JSX 提取为 `<OrdersPanel>` 组件
  3. 在 App 的 `dockContent` 中替换为 `<div style={{ display: dockTab === 'orders' ? 'block' : 'none' }}><OrdersPanel ... /></div>`
- **验证**: `npx tsc --noEmit`

### 步骤 D2: 抽取 Alerts Panel → `src/panels/AlertsPanel.tsx`

- **源码范围**: 第 2918-3060 行（`dockTab === 'alerts'` 块，包含 Alerts + Notifications 两个 section）
- **读取的外部变量**: `alerts`, `notifications`, `telegramConfig`, `notificationAction`, `telegramAction`
- **调用的外部函数**: `jumpToAlert`, `acknowledgeNotification`, `sendNotificationToTelegram`
- **引用的子组件**: `ActionButton`, `StatusPill`
- **验证**: `npx tsc --noEmit`

### 步骤 D3: Positions 和 Fills 面板 — 占位清理

- **源码范围**: 第 2916-2917 行
- **现状**: 这两行分别是 positions 和 fills 的占位 `div`，内容只有注释 `{/* missing equity */}{/* missing positions */}` 和 `{/* missing fills */}`
- **操作**:
  1. 创建 `src/panels/PositionsPanel.tsx` — 暂时只返回 `<div className="empty-state">Positions panel coming soon</div>`
  2. 创建 `src/panels/FillsPanel.tsx` — 同上
  3. 替换 main.tsx 中的占位行
- **验证**: `npx tsc --noEmit`

---

### 🔖 Block D 完成

```bash
git add -A && git commit -m "refactor: extract 4 dock panels (orders/alerts/positions/fills) from main.tsx"
```

**⏸️ 暂停，通知人工验证：点击底部 Orders / Positions / Fills / Alerts 各 tab，确认内容正常显示。**

---

## Block E — 侧边栏面板 (Side Panels) 抽取

> `sidePanelContent` prop 按 `sidebarTab` 三路条件渲染。

### 步骤 E1: 抽取 Strategy Side Panel → `src/pages/StrategySidePanel.tsx`

- **源码范围**: 第 1864-2220 行（`sidebarTab === 'strategy'` 分支内的 JSX）
- **包含内容**: 回测配置表单、回测列表、回测详情（含 Execution Trades 表格、Ledger Audit、Completed/Skipped Samples）
- **读取的外部变量**: `backtestForm`, `backtestAction`, `backtestOptions`, `strategies`, `backtests`, `backtestItems`, `selectedBacktest`, `selectedBacktestId`, `latestBacktestSummary`, `latestExecutionSource`, `latestExecutionTrades`, `latestReplayByReason`, `latestReplayCompletedSamples`, `latestReplaySkippedSamples`, `selectedSample`, `chartOverrideRange`, `selectedExecutionAvailability`, `selectedExecutionDatasets`, `selectedExecutionSymbols`, `selectedExecutionSchema`, `selectedSymbolAvailable`, `previewCountLabel`, `processedCountLabel`, `processedCountValue`, `API_BASE`
- **调用的外部函数**: `setBacktestForm`, `createBacktestRun`, `setSelectedBacktestId`, `setChartOverrideRange`, `setFocusNonce`, `setSelectedSample`, `setSourceFilter`, `setEventFilter`
- **引用的子组件**: `ActionButton`, `SimpleTable`, `SampleCard`
- **验证**: `npx tsc --noEmit`

### 步骤 E2: 抽取 Account Side Panel → `src/pages/AccountSidePanel.tsx`

- **源码范围**: 第 2222-2727 行（`sidebarTab === 'account'` 分支）
- **包含内容**:
  - Bind Account Signal Source 表单
  - Bind Strategy Signal Source 表单
  - Signal Source Catalog 表
  - Current Bindings 表
  - Runtime Policy 表单
  - Create Runtime Session 表单
  - Runtime Sessions 列表和详情（含 SignalBarChart）
- **读取的外部变量**: 非常多，包括 `accountSignalForm`, `strategySignalForm`, `signalRuntimeForm`, `runtimePolicyForm`, `signalCatalog`, `signalSourceTypes`, `signalRuntimeAdapters`, `signalRuntimeSessions`, `signalRuntimePlan`, `accountSignalBindings`, `strategySignalBindings`, `liveAccounts`, `strategyOptions`, `selectedSignalRuntime`, `selectedSignalRuntime*` 系列, `runtimePolicy`, `signalBindingAction`, `signalRuntimeAction`, `runtimePolicyAction`
- **调用的外部函数**: `setAccountSignalForm`, `setStrategySignalForm`, `setSignalRuntimeForm`, `setRuntimePolicyForm`, `bindAccountSignalSource`, `bindStrategySignalSource`, `createSignalRuntimeSession`, `updateRuntimePolicy`, `runSignalRuntimeAction`, `setSelectedSignalRuntimeId`
- **引用的子组件**: `ActionButton`, `SimpleTable`, `StatusPill`, `SignalBarChart`

> [!WARNING]
> 这个面板引用变量最多（约 40+ 个 props），建议考虑把 props 分为几个分组对象传递（如 `accountSignalProps`, `strategySignalProps`, `runtimeProps`），或者让组件内部直接读取 store。
> **推荐做法**：让组件内部直接调用 `useUIStore` 和 `useTradingStore`，减少 props 传递。

- **验证**: `npx tsc --noEmit`

---

### 🔖 Block E 完成

```bash
git add -A && git commit -m "refactor: extract strategy and account side panels from main.tsx"
```

**⏸️ 暂停，通知人工验证：切换 sidebar 到 Strategy / Account 标签，确认回测配置、信号绑定、Runtime Session 等面板正常渲染和交互。**

---

## Block F — 主舞台 (Main Stage) 页面抽取 + App 壳层瘦身

> `mainStageContent` prop 按 `sidebarTab` 三路条件渲染三套完整页面。

### 步骤 F1: 抽取 Monitor Stage → `src/pages/MonitorStage.tsx`

- **源码范围**: 第 3064-3132 行（`sidebarTab === 'monitor'` 分支）
- **包含内容**: 主监控 K 线、会话状态指标、盘口数据
- **读取的外部变量**: `monitorMode`, `monitorBars`, `monitorMarkers`, `monitorSignalState`, `monitorMarket`, `monitorSummary`, `monitorExecutionSummary`, `monitorSession`
- **引用的子组件**: `SignalMonitorChart`
- **验证**: `npx tsc --noEmit`

### 步骤 F2: 抽取 Strategy Stage → `src/pages/StrategyStage.tsx`

- **源码范围**: 第 3133-3343 行（`sidebarTab === 'strategy'` 分支）
- **包含内容**: 策略创建表单、策略参数编辑器、策略列表、当前版本摘要
- **读取的外部变量**: `strategies`, `strategyOptions`, `strategyCreateForm`, `strategyCreateAction`, `strategyEditorForm`, `strategySaveAction`, `selectedStrategy`, `selectedStrategyVersion`, `selectedStrategyParameters`, `signalRuntimeAdapters`
- **调用的外部函数**: `setStrategyCreateForm`, `createStrategy`, `setStrategyEditorForm`, `saveStrategyParameters`, `setSelectedStrategyId`
- **引用的子组件**: `ActionButton`, `SimpleTable`
- **验证**: `npx tsc --noEmit`

### 步骤 F3: 抽取 Account Stage (Overview + Live Trading) → `src/pages/AccountStage.tsx`

- **源码范围**: 第 3344-3939 行（default 分支，**最大的一块 ~600 行**）
- **包含内容**:
  - Hero 区域（含用户信息、退出、设置弹出菜单）
  - Live Trading section（含 Highlighted Live Session, Live Sessions 列表卡片, Live Accounts 卡片, Accepted Live Orders 表）
- **读取的外部变量**: 几乎全部与 live trading 相关的变量（40+ 个）
- **调用的外部函数**: `logout`, `openLiveAccountModal`, `openLiveBindingModal`, `openLiveSessionModal`, `launchLiveFlow`, `runLiveSessionAction`, `dispatchLiveSessionIntent`, `syncLiveSession`, `deleteLiveSession`, `syncLiveAccount`, `syncLiveOrder`, `jumpToSignalRuntimeSession`, `runLiveNextAction`, `selectQuickLiveAccount`, `setSettingsMenuOpen`, `setActiveSettingsModal`

> [!IMPORTANT]
> 这是最大也是最复杂的一块。如果 props 过多，**强烈建议让组件内部直接调用 useUIStore / useTradingStore**，仅通过 props 传递 **action handler 函数**（因为 action 函数依赖闭包上下文中的 `loadDashboard` 等）。
> 或者，也可以把 action handlers 也抽取到一个自定义 hook `useAppActions()` 中（但那属于更进一步的优化，不在本轮范围内）。

- **验证**: `npx tsc --noEmit`

### 步骤 F4: 瘦身 App 组件

经过以上所有步骤，`main.tsx` 中的 `App()` 函数应该只剩下：
1. ~180 行 store 解构（第 89-265 行）
2. ~340 行 derived state（第 266-605 行）
3. ~225 行 `loadDashboard()`（第 607-831 行）
4. ~165 行 useEffect/useMemo（第 833-998 行）
5. ~820 行 action handlers（第 1001-1821 行）
6. ~50 行精简后的 JSX return（组装 Layout + 各子组件）

**此步暂不需要进一步拆分**——先让上面的视图层全部稳定后，后续再评估是否需要把 action handlers 和 derived state 抽到自定义 hooks 中。

- **操作**: 清理 `main.tsx` 中不再使用的 import，删除冗余空行
- **验证**: `npx tsc --noEmit` + `npm run dev` 启动验证

---

### 🔖 Block F 完成

```bash
git add -A && git commit -m "refactor: extract 3 main stage pages, finalize main.tsx shell cleanup"
```

**⏸️ 暂停，通知人工验证：完整测试所有页面和交互。**

---

## 🗂️ 执行总览

| Block | 步骤 | 内容 | 预计减少行数 |
|---|---|---|---|
| **A** | A1-A9 (9 步) | auth 清理 + fetchJSON + ErrorBoundary + 6 个 UI 组件 | ~210 行 |
| **B** | B1-B3 (3 步) | 3 个图表组件 | ~265 行 |
| **C** | C1-C5 (5 步) | 5 个 Modal 弹窗 | ~510 行 |
| **D** | D1-D3 (3 步) | 4 个 Dock 底部面板 | ~340 行 |
| **E** | E1-E2 (2 步) | 2 个侧边栏面板 | ~860 行 |
| **F** | F1-F4 (4 步) | 3 个主舞台页面 + App 壳层清理 | ~810 行 |
| **合计** | **26 步, 6 次 git 提交** | | **~2995 行搬出，main.tsx 剩余 ~2000 行** |

> [!NOTE]
> 执行完全部 Block F 后，`main.tsx` 中还会剩下约 2000 行（主要是 store 解构 + derived state + action handlers + loadDashboard）。这部分是"逻辑层"，可以在后续阶段通过抽取自定义 hooks（如 `useAppActions`, `useDerivedState`, `useDashboardLoader`）进一步瘦身，但不在本轮范围内，因为它们需要更仔细的依赖分析。

---

## ⚙️ 每步执行的标准操作流程（给执行 LLM 的指令模板）

```
1. 阅读本计划中指定步骤的【源码范围】和【依赖列表】
2. 创建目标文件，写入 import 和 export
3. 从 main.tsx 中【剪切】对应行范围的代码，粘贴到新文件
4. 在 main.tsx 中添加新的 import 语句
5. 删除 main.tsx 中已不需要的 import（如果有）
6. 运行 `npx tsc --noEmit` 确认编译通过
7. 如果编译报错，修复 import 路径或缺失的类型引用
8. 再次 `npx tsc --noEmit` 确认清洁通过
9. 本步完成，进入下一步
```

当一个 Block 的所有步骤完成后：
运行 `git add -A && git commit -m "..."` 提交
通知人工验证
```
